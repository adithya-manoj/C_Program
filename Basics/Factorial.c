#include <stdio.h>

int factorial(int num){
       return fact*=  i;
    }


int main(){

    int i,num,fact=1;
    printf("Enter the Number: ");
    scanf("%d", &num);

    printf("%d", factorial(num));

    return 0;
}